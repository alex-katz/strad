[project]
name = "strad"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-bootswatch>=1.1",
    "mkdocs>=1.6.1",
]
